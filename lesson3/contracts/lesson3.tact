import "@stdlib/deploy";

contract Lesson3 with Deployable {
    // Empty lesson1() function is present by default in all Tact contracts
    // since v1.3.0, so the following may be omitted:
    //
    // lesson1() {}
}
